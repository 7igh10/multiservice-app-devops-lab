services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf.template:/etc/nginx/conf.d/default.conf.template:ro
    environment:
      - ACTIVE_BACKEND=${ACTIVE_BACKEND}
    command: >
      /bin/sh -c "envsubst < /etc/nginx/conf.d/default.conf.template
      > /etc/nginx/conf.d/default.conf
      && nginx -g 'daemon off;'"
    depends_on:
      - frontend
      - backend_blue
      - backend_green

  frontend:
    build: ./frontend
    image: multiservice-app-frontend:${APP_VERSION}
    expose:
      - "80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 5s
      retries: 3

  backend_blue:
    build: ./backend
    image: multiservice-app-backend:${APP_VERSION}
    expose:
      - "5000"
    environment:
      - APP_VERSION=${APP_VERSION}
      - COLOR=blue
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  backend_green:
    build: ./backend
    image: multiservice-app-backend:${NEW_VERSION}
    expose:
      - "5000"
    environment:
      - APP_VERSION=${NEW_VERSION}
      - COLOR=green
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:

env_file:
  - .env
